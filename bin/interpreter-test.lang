func main() {
    declare x : int;
    x = 5;
    if (x != 5) {
        print(-1);
    }
    else {
        print(5);
    }
    declare y : int;
    print(y);

    if (y < y + 1) {
    }
    else {
        y = x;
    }
    print(y == x);
    if (x + y) {
        if (1) {
            print(1);
        }
        else {
            print(-1);
        }
    }
    else {
        if (0) {
            print(-1);
        }
        else {
            print(0);
        }
    }

    // Loop testing:
    x = 0;
    while (x < 5) {
        print(x);
        x = x + 1;
    }

    // break testing
    x = 0;
    while (x < 5) {
        if (x == 2) { break; } else {}
        x = x + 1;
    }
    print(x);

    // continue testing
    x = 0;
    while (x < 5) {
        x = x + 1;
        if (x % 2 == 1) { continue; } else {}
        print(x);
    }
    
    // print(1 / 0);
}